const config = {
  plugins: {
    tailwindcss: {},

    // https://www.npmjs.com/package/postcss-preset-mantine
    "postcss-preset-mantine": {
      autoRem: true,
    },
  },
};

export default config;
